version: '3.1'

services:
  web:
    build: .
    ports:
      - "8080:8080"
    env_file:
      .env


  mongodb:
    image: mongo:latest
    restart: always
    ports:
      - "27017:27017"
    env_file:
      .env 
    volumes:
      - ~/data:/data/db 
