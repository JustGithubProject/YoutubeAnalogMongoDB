version: '3.1'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      .env
    depends_on:
      - mongodb


  mongodb:
    image: mongo:latest
    restart: always
    ports:
      - "27017:27017"
    env_file:
      .env 
    volumes:
      - ~/data:/data/db 
